name: Run Python on self-hosted runner

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Check files
        run: dir

      - name: Run python file
        run: python hello.py
